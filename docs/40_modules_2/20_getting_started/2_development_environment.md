# Setting up your Development Environment
Configuring the local development environment will ensure that you will have a smooth experience when using AOH's modules with the `web base`.

- GitHub PAT
- npmrc
- GHCR auth setup
- podman(?)
- VSCode Extensions(?)
- Keycloak setup